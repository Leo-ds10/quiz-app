---
services:
  db:
    image: postgres:18-alpine
    volumes:
      - db:/var/lib/postgresql/data
    env_file:
      - .env.compose
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      start_period: 1m
    restart: always

volumes:
  db:
